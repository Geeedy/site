# Local secrets for leads bot / form API (not committed).
# Copy from example and fill in:
#   TELEGRAM_BOT_TOKEN=...
#   LEADS_BOT_PASSWORD=skill-def-get-leads
#   LEADS_DATA_DIR=./data

TELEGRAM_BOT_TOKEN=
LEADS_BOT_PASSWORD=skill-def-get-leads
LEADS_DATA_DIR=
